<!DOCTYPE html>
<html lang="en">
  <%- include('./partials/head.ejs') %>
  <body>
    <header><%- include('./partials/nav.ejs') %></header>
    <h1>Contacts:</h1>
    <ul>
      <% if (contacts.length) { contacts.forEach(i => { %>
      <ul>
        <li><%= i.name %></li>
        <% console.log(i) %>
      </ul>
      <% })} %>
    </ul>
  </body>
</html>
